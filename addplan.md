# ClassSpace Manager 改良実装プロンプト

> このファイル全体を、開発担当者またはAIコーディングエージェントへの実装プロンプトとして使用する。

## 0. エージェントへの実行指示

あなたは、ClassSpace Managerの設計・実装・検証を担当するシニアソフトウェアエンジニアである。以下の要件を読み、現状のコードを調査したうえで、動作する機能を段階的に実装すること。

### 0.1 最終目的

学校内の教室を安全かつ分かりやすく検索・予約・承認できるWebアプリケーションを完成させること。

対象システム：

- リポジトリ：`Rino-program/kyoukanapp`
- フロントエンド：HTML、Vanilla JavaScript、CSS
- バックエンド：PHP 8.x、PDO
- データベース：MySQLまたはMariaDB
- 本番フロントエンド：GitHub Pages
- 本番API：XServer
- 認証：Google Identity Services
- 開発用：外部サービスを使わないデバッグ／ローカルモード

### 0.2 作業開始時に必ず行うこと

1. リポジトリのファイル構成を確認する。
2. `README.md`、`allplan.md`、本ファイル、既存のJavaScript、PHP、SQLを読む。
3. `git status`で利用者の未コミット変更を確認する。
4. 既存機能と未実装機能を整理する。
5. 変更対象を決めてから編集する。
6. 同じ機能を実現する既存コードやヘルパーを検索し、重複実装を避ける。

### 0.3 実装ルール

- 要件を満たすために必要なコードを実際に変更する。計画だけで終了しない。
- 関係のないファイルや既存仕様を変更しない。
- 既存の未コミット変更を削除、上書き、巻き戻ししない。
- 既存の命名、書式、画面構成、API形式を可能な限り維持する。
- UIだけでなくAPI側でも認証、権限、入力値、対象データを検証する。
- 画面側の検証を、サーバー側の検証の代替にしない。
- SQLはプリペアドステートメントを使用する。
- エラーを黙って無視しない。利用者向けメッセージとサーバーログを適切に分ける。
- 広範囲なcatchや成功扱いのフォールバックを追加しない。
- 本番用の秘密情報、パスワード、トークンをソースコードへ追加しない。
- HTMLへ値を出力するときはエスケープする。
- 日時はAsia/Tokyoを基準に扱う。
- 変更したコードには必要に応じてテストまたは検証を追加する。

### 0.4 1回の作業単位

各機能は、次の順序で完了させること。

1. 要件と現状コードの対応を確認する。
2. 必要なDB変更を先に設計する。
3. APIを実装する。
4. フロントエンドを実装する。
5. デバッグモードにも同じ操作を実装する。
6. 権限別に画面とAPIを確認する。
7. エラーケースを確認する。
8. 構文チェック、既存テスト、対象機能の動作確認を行う。
9. 変更ファイルと残課題を報告する。

### 0.5 判断に迷った場合の優先順位

要件が衝突した場合は、次の順で優先する。

1. データの安全性と予約の整合性
2. 認証・認可・個人情報保護
3. 明示された業務ルール
4. 既存ユーザーの操作互換性
5. UIの便利さ
6. 実装の簡潔さ

仕様が決められていない重要な業務判断は勝手に確定せず、候補、影響、推奨案を提示して確認を求める。ただし、検証用データや画面状態など、可逆的で影響の小さい事項は安全な既定値で実装する。

### 0.6 実装完了時の報告形式

作業完了時は、次の形式で簡潔に報告する。

```text
## 実装結果
- 実装した機能：
- 変更したファイル：
- DB変更：
- デバッグモードでの確認：

## 検証結果
- 実行したコマンド：
- 成功した確認：
- 未実行または環境上できなかった確認：

## 残課題
- 未実装：
- 判断待ち：
- 本番反映時の注意：
```

---

### 0.7 実装の開始点と優先順位

特に指定がない場合は、次の順番で実装する。各段階を動作確認してから次へ進む。

1. フェーズ0：基盤整理
2. フェーズ1：予約詳細、編集、複製、キャンセル、重複検証
3. フェーズ2：申請一覧、承認、却下、通知
4. フェーズ3：空き教室検索、絞り込み、レスポンシブUI
5. フェーズ4：グループメンバー管理、教室管理、ユーザー管理、設定
6. フェーズ5：定期予約、自動割り当て、メール、統計、連携

一度の変更量が大きくなりすぎる場合は、フェーズ内をさらに小さな単位へ分割する。未実装の機能を仮の成功レスポンスだけで済ませず、UI、API、データ保存、権限、エラー処理まで一通り接続する。

作業対象が指定されている場合は、その対象を優先する。対象が指定されていない場合は、最優先の未完了タスクを選び、選択理由を最終報告に含める。ドキュメントだけを更新する依頼を除き、実装可能なコード変更まで行う。

### 0.8 完了条件

機能を「完了」と報告できるのは、次の条件を満たした場合だけとする。

- 要件に対応する画面がある
- 必要なAPIがある
- 必要なデータ保存がある
- 権限ごとの許可・拒否が実装されている
- 入力エラーと通信エラーが表示される
- デバッグモードで外部サービスなしに確認できる
- 本番モードの既存動作を壊していない
- 対象の構文チェック、テスト、または手動確認を実行している
- できなかった検証を隠さず報告している

### 0.9 返答言語

ユーザーへの説明、実装結果、エラー、確認事項は日本語で記述する。コード内の既存言語、APIの識別子、DBの列名は、互換性が必要な場合に変更しない。

## 1. 文書の目的

本書は、教室の検索・予約・申請・グループ管理を行うClassSpace Managerを、学校内で継続的に利用できる実用的なシステムへ発展させるための追加計画である。

現在の構成は、GitHub Pagesで配信するHTML/JavaScriptフロントエンド、PHP REST API、MySQLデータベース、Google認証で構成されている。開発時にはデバッグモードを利用し、外部APIとGoogle認証を使わず、ブラウザ内のデモデータだけで確認できる。

改良では、次の方針を重視する。

- 生徒・部長・先生・管理者が迷わず使えること
- 予約の重複や承認漏れを防ぐこと
- 重要な操作の履歴を追跡できること
- 本番環境とデバッグ環境を安全に分離すること
- 小さな機能単位で実装・検証できること
- 既存のデータと予約ルールを壊さないこと

---

## 2. 現状整理

### 2.1 現在実装されている主な機能

- 教室一覧の取得
- 日付ごとのタイムライン表示
- 通常予約の作成
- 予約のキャンセル
- 使用中教室への相席・譲渡申請
- グループの一覧表示・作成・削除
- 通知一覧の取得
- Googleログイン
- 役割による権限制御
- PHP APIとMySQLによる本番処理
- URLの`?debug=1`で有効化できるローカルデバッグモード
- デバッグモードでの予約・グループデータのlocalStorage保存

### 2.2 現状の課題

- 予約の編集、複製、定期登録が不足している
- 申請一覧や承認・却下画面が十分に実装されていない
- グループのメンバー管理が不足している
- 管理者向けの教室・ユーザー・設定画面が不足している
- 予約を探すための検索・絞り込み機能が少ない
- 読み込み中、通信失敗、権限不足などの状態表示が不十分である
- デバッグモードで複数ユーザーや各権限を再現しにくい
- API設定や本番用秘密情報の管理方法を改善する必要がある
- 監査ログ、通知履歴、操作履歴が不足している
- 大量データや同時予約を想定した検証が不足している

---

## 3. 利用者と権限

### 3.1 開発者（developer）

全機能を利用できる。デバッグ設定、システム設定、操作ログ、データ保守、検証用ユーザー切り替えを行う。

### 3.2 管理者（admin）

教室、ユーザー、グループ、予約、申請を管理する。予約の強制変更、教室の閉鎖、ユーザー権限変更を行う。

### 3.3 先生（teacher）

担当教室の責任者として申請を承認・却下する。必要に応じて担当教室の予約を変更できる。

### 3.4 部長（leader）

所属グループの予約、定期予約、自動割り当て、メンバー管理、他グループへの申請を行う。

### 3.5 生徒（student）

空き教室の検索、単発予約、臨時グループ作成、相席・譲渡申請を行う。

### 3.6 権限設計上の注意

- 画面上でボタンを隠すだけでなく、API側でも必ず権限を確認する
- developerとadminの権限を必要以上に共有しない
- 予約の所有者、グループ所属、教室責任者を確認する
- デバッグユーザーは本番APIへ送信できないようにする
- 権限変更は操作ログへ記録する

---

## 4. 優先順位と開発フェーズ

### フェーズ0：基盤整理

目的は、今後の改良を安全に進められる状態にすることである。

- API通信処理を整理する
- 本番、ローカル、デバッグの切り替えを明確化する
- 共通エラー形式を統一する
- loading、empty、error状態を共通化する
- DBの外部キーとインデックスを確認する
- API設定から秘密情報を分離する
- 主要画面の動作確認手順を文書化する

完了条件：

- `?debug=1`で外部通信が発生しない
- 通常モードでは本番APIへ接続できる
- 主要APIのエラーが画面に表示される
- JavaScriptとPHPの構文チェックが通る

### フェーズ1：予約機能の完成

最も利用頻度が高い予約処理を優先する。

- 予約詳細画面
- 予約編集
- 予約複製
- 予約キャンセル
- 重複チェック
- 利用時間チェック
- 予約可能日数チェック
- 予約履歴

### フェーズ2：申請・通知ワークフロー

申請を送るだけで終わらず、承認者が処理できる状態にする。

- 申請一覧
- 承認・却下
- 却下理由
- 申請詳細
- 状態バッジ
- アプリ内通知
- 未読件数

### フェーズ3：検索とUI改善

利用者が空き教室を短時間で見つけられるようにする。

- 空き教室検索
- 建物、定員、設備による絞り込み
- 日付・時間帯指定
- カレンダー表示
- スマートフォン対応
- ローディング表示

### フェーズ4：グループ・管理者機能

- メンバー管理
- 教室管理
- ユーザー管理
- システム設定
- 操作ログ

### フェーズ5：高度な機能

- 定期予約
- 自動割り当て
- メール通知
- カレンダー連携
- PWA・オフライン対応
- 利用統計

---

## 5. 予約機能の詳細計画

### 5.1 予約詳細画面

予約をクリックしたときに、次の情報を表示する。

- 教室名
- 建物
- 定員
- 予約者
- グループ名
- 開始時刻
- 終了時刻
- 用途
- 予約種別
- 状態
- 作成日時
- 更新日時

利用者本人には編集・キャンセルボタンを表示する。先生、admin、developerには権限に応じて強制変更ボタンを表示する。

### 5.2 予約編集

編集対象は次の項目とする。

- 教室
- 開始時刻
- 終了時刻
- 用途
- グループ
- 予約種別

編集時も新規予約と同じ検証を行う。

- 開始より終了が後であること
- 授業時間帯と重ならないこと
- 既存の別予約と重ならないこと
- 予約可能日数内であること
- 利用者が編集権限を持つこと

自分自身の予約を重複チェックから除外する必要がある。

### 5.3 予約複製

過去の予約を元に、別の日付へ同じ条件で予約を作成する。

- 元の教室を初期選択する
- 元の開始・終了時刻を引き継ぐ
- 用途とグループを引き継ぐ
- 日付だけ変更できる
- 作成前に重複チェックを行う

### 5.4 定期予約

部活動など、毎週同じ時間に利用する場合に使用する。

設定項目：

- 曜日
- 開始日
- 終了日
- 開始時刻
- 終了時刻
- 教室
- グループ
- 用途
- 予約種別

一部の日付で重複または授業時間帯との衝突がある場合は、次のいずれかを選択できるようにする。

- 全件を中止
- 空いている日だけ作成
- 衝突した日を一覧表示して利用者が選択

定期予約は、1件の親予約と複数の実予約を分けて管理すると、編集・キャンセルが容易になる。

### 5.5 予約キャンセル

キャンセル時には確認画面を表示する。

- 教室名
- 日時
- 用途
- キャンセル理由（任意または必須）

キャンセル後は物理削除せず、`cancelled`状態として履歴を残す。

### 5.6 空き教室検索

検索条件：

- 利用日
- 開始時刻
- 終了時刻
- 建物
- 最低定員
- 特別許可の要否
- 授業時間帯を含めるか

検索結果には次を表示する。

- 教室名
- 建物
- 定員
- 設備
- 空き状態
- 予約ボタン

---

## 6. 申請・承認機能の詳細計画

### 6.1 申請状態

- `pending`：承認待ち
- `approved`：承認済み
- `rejected`：却下
- `cancelled`：申請者が取り消し

### 6.2 申請一覧

申請センターでは、自分が申請者または承認者である申請を表示する。

表示項目：

- 申請種別
- 教室
- 対象予約
- 申請者
- 承認者
- 日時
- メッセージ
- 状態
- 作成日時

フィルター：

- 自分が送った申請
- 自分が承認する申請
- 承認待ち
- 承認済み
- 却下

### 6.3 承認処理

承認者は申請詳細を確認し、承認または却下する。

却下時は理由を入力する。承認時には、対象予約がすでにキャンセル・変更されていないかを再確認する。

相席申請では、既存予約を維持しつつ共有ユーザーまたは共有グループを登録する。

譲渡申請では、元予約を`overridden`または`cancelled`にし、申請者の予約を作成する。

### 6.4 競合対策

承認処理はトランザクション内で行う。

- 対象予約をロックする
- 対象予約がactiveか確認する
- 新しい予約との重複を確認する
- 状態変更と通知作成を同じトランザクションで行う

### 6.5 通知

通知対象：

- 申請を受けた承認者
- 承認または却下された申請者
- 予約が変更された利用者
- 管理者が強制変更を行った対象者

通知にはリンク先を持たせ、クリックすると申請または予約詳細を開けるようにする。

---

## 7. グループ管理の詳細計画

### 7.1 グループ詳細

- グループ名
- 種類
- 親グループ
- リーダー
- メンバー数
- 予約数
- 作成日時

### 7.2 メンバー管理

リーダーまたは管理者が次の操作を行う。

- メンバー追加
- メンバー削除
- リーダー変更
- メンバー権限変更
- 招待コード発行

招待コードは期限と使用回数を設定する。推測しやすい連番は避ける。

### 7.3 グループ階層

クラブを親グループ、パートを子グループとして扱う。

例：

- 吹奏楽部
  - フルートパート
  - トランペットパート

親グループのリーダーが子グループの管理を行えるかどうかは、設定として切り替えられるようにする。

### 7.4 グループ別統計

- 月別予約数
- 利用時間
- 利用教室
- キャンセル数
- 申請数

---

## 8. 管理者画面の詳細計画

### 8.1 教室管理

管理者は教室を追加・編集・閉鎖できる。

管理項目：

- 教室名
- 建物
- 階数
- 定員
- 設備
- 特別許可の要否
- 授業時間帯の閉鎖
- 責任者
- 利用停止状態
- 利用停止理由

利用停止中の教室は新規予約できない。既存予約をどう扱うかは、停止時に次の選択肢を示す。

- 既存予約を維持
- 対象予約をキャンセル
- 代替教室への移動を申請

### 8.2 ユーザー管理

- ユーザー検索
- 名前・メールアドレス表示
- 権限変更
- 利用停止
- 所属グループ確認
- 予約履歴確認

権限変更時は、変更前と変更後の権限を操作ログに記録する。

### 8.3 システム設定

既存の`system_settings`を画面から編集できるようにする。

- 授業開始時刻
- 授業終了時刻
- 一般ユーザーの予約可能日数
- 管理者の予約可能日数
- 予約の最小時間
- 予約の最大時間
- キャンセル期限
- 通知設定

設定値は文字列として保存するが、APIで型と範囲を検証する。

### 8.4 操作ログ

記録対象：

- ログイン
- ログアウト
- 予約作成
- 予約編集
- 予約キャンセル
- 申請送信
- 承認・却下
- 教室変更
- 権限変更
- 強制操作

ログには、ユーザー、操作種別、対象ID、変更前、変更後、IPアドレス、日時を記録する。

---

## 9. UI・UX改善計画

### 9.1 状態表示

すべての非同期処理で次の状態を明確にする。

- 読み込み中
- データなし
- 通信エラー
- 権限不足
- 成功

ボタン連打を防ぐため、送信中はボタンを無効化する。

### 9.2 タイムライン

- 現在時刻を縦線で表示
- 予約済み、空き、授業中、閉鎖を色で区別
- 予約の長さを視覚的に反映
- クリック可能な要素にカーソルと説明を付ける
- スマートフォンでは時間軸を横スクロールできるようにする

### 9.3 フィルター

フィルターは画面を移動しても保持する。

- 建物
- 定員
- 空きのみ
- 特別許可不要
- 授業中を除外

### 9.4 レスポンシブ対応

スマートフォンでは、次の構成を基本とする。

- 下部ナビゲーション
- 縦長カード
- ボトムシート形式の詳細画面
- 大きなタップ領域
- 横スクロールの予約表

### 9.5 アクセシビリティ

- 色だけで状態を伝えない
- ボタンに明確なラベルを付ける
- キーボード操作に対応する
- フォーカス位置を見えるようにする
- エラーメッセージを入力欄の近くに表示する
- 文字サイズとコントラストを確保する

### 9.6 ダークモード

システム設定または画面上の切り替えで変更できるようにする。

- 選択状態をlocalStorageへ保存
- 予約状態の色のコントラストを維持
- 印刷時は明るい配色に戻す

---

## 10. デバッグ・ローカル開発の詳細計画

### 10.1 モード

- 通常モード：本番APIとGoogle認証を使用
- ローカルモード：localhostまたはfileプロトコルでデモAPIを使用
- デバッグモード：`?debug=1`で明示的にデモAPIを使用

### 10.2 デバッグユーザー切り替え

次のユーザーを選択できるようにする。

- デバッグ生徒
- デバッグ部長
- デバッグ先生
- デバッグ管理者
- デバッグ開発者

切り替えた権限は、画面表示だけでなくデモAPIの権限検証にも反映する。

### 10.3 デモデータ管理

- 初期データへリセット
- 予約データをJSON出力
- JSONデータをインポート
- データ件数表示
- 全データ削除前の確認

予約・グループ・申請・通知をそれぞれ別のlocalStorageキーで管理する。

### 10.4 通信ログ

デバッグモードのみ、開発者パネルに次を表示する。

- リクエスト時刻
- HTTPメソッド
- APIパス
- リクエスト内容
- レスポンス内容
- 処理時間
- エラー内容

パスワード、トークン、個人情報はログへ出さない。

### 10.5 デバッグモードの安全策

- デバッグモード中は本番APIへ接続しない
- Googleスクリプトを読み込まない
- デバッグユーザーのIDを本番ユーザーと重複させない
- 画面上にデバッグモードであることを表示する
- 本番ビルドではデバッグパネルを無効化できるようにする

---

## 11. API改良計画

### 11.1 共通レスポンス

成功例：

```json
{
  "data": {},
  "meta": {
    "request_id": "..."
  }
}
```

エラー例：

```json
{
  "error": {
    "code": "RESERVATION_CONFLICT",
    "message": "その時間帯はすでに予約されています",
    "details": {}
  }
}
```

既存クライアントとの互換性を確認しながら段階的に移行する。

### 11.2 追加API候補

- `GET /rooms/available`
- `GET /rooms/{id}`
- `POST /reservations`
- `GET /reservations/{id}`
- `PUT /reservations/{id}`
- `DELETE /reservations/{id}`
- `POST /reservations/recurring`
- `GET /requests`
- `PUT /requests/{id}/approve`
- `PUT /requests/{id}/reject`
- `GET /groups/{id}`
- `GET /groups/{id}/members`
- `POST /groups/{id}/members`
- `DELETE /groups/{id}/members/{user_id}`
- `GET /admin/users`
- `PUT /admin/users/{id}/role`
- `GET /admin/settings`
- `PUT /admin/settings/{key}`
- `GET /admin/audit-logs`

### 11.3 入力検証

APIでは、次を必ず検証する。

- 必須項目
- 文字数
- 日付形式
- 時刻の前後関係
- 数値範囲
- 許可された列挙値
- IDの存在
- 権限
- 対象データの所有者

---

## 12. データベース改良計画

### 12.1 追加候補テーブル

#### reservation_series

定期予約の親情報を保存する。

- id
- group_id
- room_id
- start_date
- end_date
- weekday
- start_time
- end_time
- purpose
- status
- created_by

#### audit_logs

操作履歴を保存する。

- id
- user_id
- action
- resource_type
- resource_id
- before_data
- after_data
- ip_address
- created_at

#### room_facilities

教室設備を正規化して保存する。

- room_id
- facility_id

#### facilities

設備マスタを保存する。

- id
- name

#### group_invites

グループ招待を保存する。

- id
- group_id
- token_hash
- expires_at
- max_uses
- used_count
- created_by

### 12.2 インデックス

利用頻度の高い検索に対して、次のインデックスを検討する。

- reservations(room_id, status, start_time, end_time)
- reservations(user_id, created_at)
- requests(approver_user_id, status, created_at)
- requests(applicant_user_id, status, created_at)
- notifications(user_id, is_read, created_at)
- audit_logs(resource_type, resource_id, created_at)

### 12.3 データ移行

テーブル追加や列変更を行う場合は、既存データを壊さない手順を用意する。

1. 新しいテーブルまたは列を追加する
2. 既存データを移行する
3. 読み取り処理を新方式へ切り替える
4. 書き込み処理を新方式へ切り替える
5. 件数と整合性を確認する

---

## 13. 通知機能の詳細計画

### 13.1 アプリ内通知

- 未読件数をヘッダーに表示
- 通知一覧を表示
- 通知をクリックして対象画面へ遷移
- 一括既読
- 通知削除

### 13.2 メール通知

初期設定ではメール通知を無効にし、管理者が有効化できるようにする。

対象：

- 申請受信
- 承認・却下
- 予約開始前
- 予約変更
- 教室閉鎖

メール送信失敗が予約処理全体を失敗させないように、通知キューまたは送信結果記録を用意する。

### 13.3 リマインダー

予約開始の24時間前、1時間前などを設定可能にする。

同じ通知を重複送信しないため、通知済みキーを保存する。

---

## 14. セキュリティ計画

- 本番設定ファイルをGit管理対象外にする
- DBパスワード、JWT秘密鍵を環境変数またはサーバー設定へ移す
- JWT秘密鍵を十分長いランダム値にする
- HTTPSを必須にする
- CORS許可オリジンを明示的に限定する
- SQLは必ずプリペアドステートメントを使用する
- 入力値をHTMLへ出力するときはエスケープする
- ログへトークンやパスワードを記録しない
- Google IDトークンのaudienceと有効期限を検証する
- 失敗ログインや大量リクエストを制限する
- 重要操作に再確認を設ける
- 管理者操作を監査ログへ記録する

デバッグモードは利便性のための機能であり、本番認証を代替するものではない。本番環境でURLパラメータだけにより権限を変更できないようにする。

---

## 15. テスト計画

### 15.1 JavaScript

- デバッグモード判定
- 通常モードのAPI URL判定
- localStorageの読み書き
- 予約作成
- 予約キャンセル
- グループ作成・削除
- 日付フィルター
- JSON破損時のエラー処理

### 15.2 PHP API

- 未認証アクセスの拒否
- 権限不足の拒否
- 予約時間の逆転
- 予約重複
- 授業時間帯の予約拒否
- 予約可能日数超過
- 予約所有者による編集・削除
- 管理者による強制変更
- 申請承認時の競合

### 15.3 画面確認

- PC表示
- タブレット表示
- スマートフォン表示
- キーボード操作
- 長いグループ名
- 教室が0件の場合
- 予約が0件の場合
- APIエラー時
- セッション切れ時

### 15.4 受け入れ条件

- 生徒が空き教室を検索して予約できる
- 重複予約が作成されない
- 部長がグループ予約を管理できる
- 先生が担当教室の申請を処理できる
- 管理者が教室とユーザーを管理できる
- すべての重要操作が履歴に残る
- デバッグモードで外部サービスなしに主要機能を確認できる

---

## 16. 運用・バックアップ計画

- DBの定期バックアップを設定する
- バックアップの復元手順を文書化する
- 本番設定ファイルを安全に保管する
- エラーログを定期的に確認する
- 操作ログの保存期間を決める
- 退学・卒業・異動ユーザーの扱いを決める
- 使われなくなったグループと教室をアーカイブする
- 障害時の連絡先と復旧手順を決める

---

## 17. 推奨する実装順

### 最優先

1. 予約詳細・編集・キャンセルの完成
2. 予約重複チェックとエラー表示の強化
3. 申請一覧・承認・却下の実装
4. 通知バッジと未読管理
5. デバッグユーザー切り替えと初期化ボタン

### 次に実装

1. 空き教室検索と絞り込み
2. グループメンバー管理
3. 教室管理
4. ユーザー権限管理
5. システム設定画面
6. 操作ログ

### その後に実装

1. 定期予約
2. 自動割り当て
3. メール通知
4. 利用統計
5. カレンダー連携
6. PWA・オフライン対応

---

## 18. 完成時の理想的な利用フロー

### 生徒

1. ログインする
2. 日付・時刻・人数を指定する
3. 空き教室を検索する
4. 用途を入力して予約する
5. 必要なら相席・譲渡を申請する
6. 通知で結果を確認する

### 部長

1. グループを選択する
2. メンバーと予定を確認する
3. 空き教室を検索する
4. 単発または定期予約を作成する
5. 競合時は自動割り当てを実行する
6. メンバーへ予定を共有する

### 先生

1. 承認待ち申請を確認する
2. 対象教室と予約内容を確認する
3. 承認または理由付きで却下する
4. 必要に応じて担当教室を閉鎖する

### 管理者

1. 教室と責任者を管理する
2. ユーザー権限を管理する
3. 予約・申請・操作ログを確認する
4. システム設定を調整する
5. 障害や不正利用を調査する

---

## 19. まとめ

ClassSpace Managerは、まず予約・申請・通知という日常利用に直結する機能を完成させ、その後に管理者機能、定期予約、自動割り当て、統計へ拡張するのが望ましい。

特に重要なのは、次の4点である。

- 予約の正確性
- 承認状況の可視化
- 権限と操作履歴の安全性
- デバッグ環境と本番環境の分離

---

## 20. 画面仕様の詳細

### 20.1 共通レイアウト

全画面で次の構造を使用する。

1. ヘッダー
   - 現在の画面名
   - 通知ボタン
   - ログインユーザー
   - デバッグモード表示
2. メインコンテンツ
   - ページ見出し
   - 操作ボタン
   - 検索・絞り込み
   - 一覧またはフォーム
3. フィードバック領域
   - トースト通知
   - ダイアログ
   - エラー表示

画面ごとに独自の通信処理を書かず、API呼び出し、ローディング、エラー、再試行の共通処理を利用する。

### 20.2 タイムライン画面

#### 初期表示

- 今日の日付を初期選択する
- 08:00から22:00までを基本表示する
- 教室を建物名、教室名の順で並べる
- 予約済みの枠を予約カードとして表示する
- 授業時間帯の閉鎖教室をロック表示する

#### 操作

- 前日・翌日へ移動
- 今日へ戻る
- 日付ピッカーで任意の日付へ移動
- 空き教室検索を開く
- 予約カードをクリックして詳細を開く
- 空き枠をクリックして予約フォームを開く
- 建物・定員・空き状態で絞り込む

#### 表示ルール

- 空き：予約可能を示す薄い背景
- 予約済み：予約者名とグループ名を表示
- 授業中：予約不可であることを表示
- 教室閉鎖：閉鎖理由と終了時刻を表示
- 自分の予約：本人用の強調表示を行う

### 20.3 予約フォーム

#### 入力項目

| 項目 | 必須 | 検証 |
|---|---:|---|
| 教室 | ○ | 存在する教室であること |
| 利用日 | ○ | 許可された範囲であること |
| 開始時刻 | ○ | 営業時間内であること |
| 終了時刻 | ○ | 開始より後であること |
| 用途 | ○ | 1〜255文字 |
| グループ | 任意 | 所属グループのみ選択可能 |
| 予約種別 | ○ | normal、recurring、event_priorityのいずれか |

#### 送信前確認

送信前にサーバーへ空き状況を問い合わせ、次を表示する。

- 選択した教室
- 利用日時
- 用途
- グループ
- 予約可能かどうか
- 注意事項

サーバーの最終検証は必須であり、画面側の検証だけで予約を確定してはならない。

### 20.4 予約詳細ダイアログ

#### 権限別ボタン

| 操作者 | 閲覧 | 編集 | キャンセル | 強制変更 |
|---|---:|---:|---:|---:|
| 予約者 | ○ | ○ | ○ | - |
| 同じグループのメンバー | ○ | - | - | - |
| 担当先生 | ○ | 条件付き | 条件付き | - |
| 管理者 | ○ | ○ | ○ | ○ |
| 開発者 | ○ | ○ | ○ | ○ |

「条件付き」は、担当教室または担当グループとして登録されている場合に限る。

### 20.5 申請センター画面

画面を「受信した申請」「送信した申請」「履歴」に分ける。

- 受信した申請：自分が承認者となる申請
- 送信した申請：自分が申請者となる申請
- 履歴：処理済みの申請

承認待ちの申請には未処理バッジを表示する。却下理由がある場合は一覧上でも確認できるようにする。

### 20.6 グループ画面

#### 一覧

- グループ名
- グループ種別
- 自分の役割
- メンバー数
- 今後の予約数

#### 詳細

- 基本情報
- メンバー一覧
- 今後の予約
- 過去の予約
- 招待管理
- グループ設定

### 20.7 管理者ダッシュボード

初期表示で次の集計を表示する。

- 今日の予約件数
- 承認待ち申請数
- 利用停止中の教室数
- 今月のキャンセル数
- アクティブユーザー数
- エラー件数

各集計をクリックすると、対象一覧へ移動できるようにする。

---

## 21. 業務ルールの詳細

### 21.1 予約可能時間

システム設定から次の値を取得する。

- 通常の利用開始時刻
- 通常の利用終了時刻
- 授業開始時刻
- 授業終了時刻
- 最小予約時間
- 最大予約時間

設定が取得できない場合は、予約を安全側に倒して停止し、管理者へエラーを表示する。推測した初期値で予約を作成してはならない。

### 21.2 予約の重複

同じ教室について、次の条件を満たす予約は重複と判定する。

```text
既存.start_time < 新規.end_time
かつ
既存.end_time > 新規.start_time
かつ
既存.status = active
```

終了時刻と次の開始時刻が同じ場合は、連続利用として許可する。

### 21.3 教室閉鎖

教室が閉鎖されている場合、新規予約と予約変更を禁止する。

- 定期予約の作成も禁止する
- 既存予約を自動削除しない
- 既存予約への影響を管理者に表示する
- 閉鎖解除時は通常どおり予約可能に戻す

### 21.4 予約の変更

予約変更では、次の順序で処理する。

1. 認証確認
2. 権限確認
3. 対象予約のロック
4. 対象予約がactiveであることを確認
5. 入力値の検証
6. 教室閉鎖の確認
7. 重複確認
8. 更新
9. 操作ログ作成
10. 通知作成
11. コミット

### 21.5 キャンセル期限

キャンセル期限を過ぎた予約は、一般利用者がキャンセルできないようにする。

- 期限内：本人がキャンセル可能
- 期限超過：先生または管理者へ申請
- 管理者：理由を記録して強制キャンセル可能

### 21.6 申請の有効期限

申請には有効期限を持たせる。期限を過ぎた申請は自動的にcancelledへ変更し、承認できないようにする。

### 21.7 時刻とタイムゾーン

- DBはAsia/Tokyo基準で保存する
- APIの日時形式を統一する
- 入力はローカル時刻として受け取る
- 表示時には利用者の環境に依存せず学校のタイムゾーンを優先する
- 夏時間を前提にしない

---

## 22. API仕様の詳細

### 22.1 認証ヘッダー

本番APIでは次の形式を使用する。

```http
Authorization: Bearer <jwt>
```

トークンには次のクレームを含める。

- `sub`：ユーザーID
- `role`：権限
- `iat`：発行時刻
- `exp`：有効期限
- `jti`：必要に応じたトークン識別子

サーバー側では署名、有効期限、ユーザーの利用停止状態を検証する。

### 22.2 予約API

#### `GET /reservations`

クエリ：

- `date`
- `room_id`
- `group_id`
- `status`
- `from`
- `to`

レスポンスに含める項目：

- 予約ID
- 教室情報
- グループ情報
- 予約者情報
- 開始・終了日時
- 用途
- 種別
- 状態

#### `POST /reservations`

リクエスト例：

```json
{
  "room_id": 1,
  "group_id": 3,
  "start_time": "2026-08-28T16:00:00+09:00",
  "end_time": "2026-08-28T18:00:00+09:00",
  "reservation_type": "normal",
  "purpose": "合奏練習"
}
```

重複時はHTTP 409を返し、エラーコードを`RESERVATION_CONFLICT`とする。

#### `PUT /reservations/{id}`

部分更新を許可する。ただし、日時・教室・グループを変更する場合は予約全体の再検証を行う。

#### `DELETE /reservations/{id}`

物理削除をせず、cancelledへ変更する。すでにcancelledの場合は冪等に成功扱いにするか、明確なエラーコードを返す。

### 22.3 教室API

#### `GET /rooms/available`

検索条件を受け取り、予約可能な教室だけを返す。

必須条件：

- 開始日時
- 終了日時

任意条件：

- 最低定員
- 建物
- 設備
- 特別許可不要

### 22.4 申請API

#### `POST /requests`

申請種別ごとに必須項目を変える。

- permission：教室、希望日時、メッセージ
- share：対象予約、希望日時、メッセージ
- transfer：対象予約、希望日時、メッセージ

#### `PUT /requests/{id}/approve`

承認者本人または管理者のみ実行可能とする。

#### `PUT /requests/{id}/reject`

却下理由を必須にする。理由は1〜1000文字とする。

### 22.5 ページング

一覧APIには次のパラメータを持たせる。

- `page`
- `per_page`
- `sort`
- `order`

`per_page`には上限を設定し、極端に大きな値を受け付けない。

### 22.6 冪等性

予約作成や定期予約作成では、通信再送による二重登録を防ぐため、必要に応じて`Idempotency-Key`を受け付ける。

同じキーと同じ利用者による再送は、最初の結果を返す。

---

## 23. データモデルの詳細

### 23.1 users

追加候補：

- `is_active`
- `last_login_at`
- `suspended_at`
- `suspension_reason`

メールアドレスは検索用インデックスを付ける。画面に表示する際は、一般ユーザーへ不要な個人情報を公開しない。

### 23.2 rooms

追加候補：

- `floor`
- `description`
- `is_active`
- `closed_reason`
- `updated_at`

教室名と建物の組み合わせは重複しないようにする。

### 23.3 reservations

追加候補：

- `series_id`
- `cancelled_by`
- `cancelled_at`
- `cancellation_reason`
- `updated_at`
- `version`

`version`を楽観的ロックに利用し、古い画面からの上書きを防止する。

### 23.4 requests

追加候補：

- `expires_at`
- `processed_at`
- `processed_by`
- `decision_comment`

### 23.5 notifications

追加候補：

- `notification_type`
- `entity_type`
- `entity_id`
- `read_at`
- `delivery_status`

リンクURLを直接組み立てず、対象種別と対象IDから安全に生成する。

### 23.6 audit_logs

変更前後のJSONは個人情報を必要以上に含めない。保存期間を定め、定期的にアーカイブまたは削除する。

---

## 24. エラー処理と復旧

### 24.1 利用者向けエラー

利用者には原因と次の行動を示す。

- 重複：別の時間または教室を選ぶ
- 権限不足：担当者へ申請する
- セッション切れ：再ログインする
- 通信失敗：再試行する
- 入力エラー：該当欄を修正する

内部SQLエラー、ファイルパス、トークン情報は表示しない。

### 24.2 再試行

GETは安全に再試行できる。POST、PUT、DELETEは、処理結果が不明な場合に自動再送せず、冪等性キーまたは利用者確認を用いる。

### 24.3 セッション切れ

401を受け取った場合：

1. 現在の入力内容を可能な範囲で保持
2. ログイン画面またはログイン案内を表示
3. 再ログイン後に自動送信しない
4. 利用者が内容を確認して再送する

### 24.4 データ不整合

予約に存在しない教室、グループ、ユーザーが紐付いている場合は、通常画面で黙って補完しない。

- 管理者に警告する
- 予約の表示自体は可能な範囲で行う
- 修復操作を監査ログに残す

---

## 25. 実装タスクの分解

### 25.1 基盤タスク

- APIクライアントを共通化する
- JSONエラー形式を定義する
- loading表示を共通化する
- toast表示を共通化する
- 確認ダイアログを共通化する
- 権限判定ヘルパーを作成する
- 日時フォーマットヘルパーを作成する
- HTMLエスケープ処理を統一する

### 25.2 予約タスク

- 予約詳細モーダル
- 予約編集フォーム
- 予約複製フォーム
- 予約履歴API
- 予約の重複エラー表示
- 予約キャンセル理由
- 予約の楽観的ロック

### 25.3 申請タスク

- 申請一覧API
- 申請状態フィルター
- 申請詳細画面
- 承認API
- 却下API
- 申請期限処理
- 通知連携

### 25.4 グループタスク

- グループ詳細API
- メンバー一覧API
- メンバー追加・削除
- リーダー変更
- 招待コード
- グループ別予約一覧

### 25.5 管理者タスク

- 教室CRUD
- 教室閉鎖処理
- ユーザー検索
- 権限変更
- システム設定CRUD
- 監査ログ一覧
- 管理者用ダッシュボード

### 25.6 デバッグタスク

- デバッグユーザー切り替え
- 権限別表示確認
- データリセット
- JSONインポート・エクスポート
- 通信ログ
- デバッグモード表示
- 外部通信禁止チェック

---

## 26. リリース手順の詳細

### 26.1 開発環境

1. デバッグモードで画面を確認する
2. 権限ごとの操作を確認する
3. JavaScript構文チェックを実行する
4. PHP構文チェックを実行する
5. APIの主要エラーケースを確認する
6. 変更内容をレビューする

### 26.2 ステージング

1. 本番と同じPHPバージョンで動かす
2. テスト用DBを使用する
3. Google認証の許可オリジンを分離する
4. 本番の秘密情報を使わない
5. 予約重複と申請処理を確認する
6. バックアップからの復元を確認する

### 26.3 本番

1. DBバックアップを取得する
2. DB変更を先に適用する
3. APIをデプロイする
4. フロントエンドをデプロイする
5. ヘルスチェックを実行する
6. テストユーザーでログインする
7. 教室一覧と予約一覧を確認する
8. 問題がなければ利用者へ告知する

### 26.4 ロールバック

ロールバック対象を分ける。

- フロントエンドだけ戻す
- APIだけ戻す
- DB変更を戻す

DBの破壊的変更は避け、旧APIと新APIが一時的に共存できる移行方式を採用する。

---

## 27. 監視項目と運用指標

### 27.1 技術指標

- API応答時間
- HTTP 4xx件数
- HTTP 5xx件数
- DB接続エラー数
- 予約競合件数
- 通知送信失敗数
- ログイン失敗数

### 27.2 業務指標

- 日別予約数
- 教室利用率
- キャンセル率
- 申請承認までの平均時間
- 利用者数
- よく使われる教室
- 時間帯別の混雑度

### 27.3 アラート

次の状態を管理者へ通知する。

- 5xxエラーが一定数を超えた
- DB接続に失敗した
- バックアップが失敗した
- 通知キューが滞留した
- 短時間にログイン失敗が増えた
- 同じ教室で予約競合が急増した

---

## 28. 利用者向けヘルプと告知

### 28.1 初回利用案内

初回ログイン時に次を説明する。

- 教室の探し方
- 予約の作り方
- キャンセル方法
- 相席・譲渡申請
- 通知の確認方法
- 問い合わせ先

### 28.2 操作説明

各画面に短いヘルプを表示する。長い説明はヘルプページへ分離し、操作中に必要な情報だけを表示する。

### 28.3 ルール変更の告知

予約可能日数、授業時間、キャンセル期限を変更した場合は、変更前後の値と適用日時を通知する。

---

## 29. 今後の判断が必要な項目

実装前に学校の運用担当者と次を決定する。

- 予約可能な時間帯
- 一般ユーザーの予約可能日数
- 予約の最大時間
- キャンセル期限
- 申請の有効期限
- 先生の承認範囲
- 親グループと子グループの権限関係
- メール通知を有効にするか
- 操作ログの保存期間
- 卒業・異動後のデータ保持期間
- 教室閉鎖時の既存予約の扱い
- 個人情報の表示範囲
- 管理者の人数と引き継ぎ方法

これらは技術だけで決めず、実際の学校運用と個人情報保護の方針に合わせる。

---

## 30. 最終的な完成判定

次の条件をすべて満たした時点を、第一段階の完成とする。

### 機能

- 全権限のログインと権限確認ができる
- 教室を検索できる
- 予約を作成、表示、編集、キャンセルできる
- 重複予約が防止される
- 申請を送信、承認、却下できる
- 通知を確認できる
- グループとメンバーを管理できる
- 管理者が教室、ユーザー、設定を管理できる

### 品質

- 主要なAPIエラーが利用者へ分かりやすく表示される
- 同時操作でデータが壊れない
- 重要操作が監査ログに記録される
- スマートフォンでも主要操作ができる
- デバッグモードで外部サービスなしに確認できる

### 運用

- バックアップと復元手順がある
- 本番秘密情報がソースコードに含まれない
- 障害時の連絡先と切り戻し手順がある
- 管理者向けの操作説明がある
- 利用者向けのヘルプがある

以上を満たしたうえで、定期予約、自動割り当て、統計、カレンダー連携、PWA化を第二段階の拡張として進める。

この順序で開発すれば、利用者にとって分かりやすく、管理者にとって運用しやすく、将来の機能追加にも耐えられる構成になる。
